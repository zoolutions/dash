# frozen_string_literal: true

# Generated Configuration page for lib/dash/configuration/docs/role.yml —
# content parsed by ConfigDoc, rendered by ConfigPage.
class Views::Docs::Pages::Config::Role < Views::Docs::Pages::ConfigPage
  config_doc "role"

  def lead = "Different server types — web, workers — each with its own config."
end
